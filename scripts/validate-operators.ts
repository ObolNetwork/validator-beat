/**
 * Validates operator YAML under data/operators/.
 * Full Zod schema + rubric checks ship with the data pipeline task.
 */
console.log(
  "validate-operators: not implemented yet — add Zod schema in lib/schemas/",
);
